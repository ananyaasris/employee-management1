server:
  port: 8084
  servlet:
    context-path: /gatewayservice

spring:
  cloud:
    config:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,mappings
  endpoint:
    health:
      show-details: always
