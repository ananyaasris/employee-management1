server:
  port: 8084

spring:
  application:
    name: gateway-service

  cloud:
    config:
      enabled: false
      import-check:
        enabled: false

  main:
    web-application-type: reactive

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
