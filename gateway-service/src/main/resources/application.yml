server:
  port: 8084

spring:
  cloud:
    gateway:
      routes:
        - id: employee-service
          uri: http://localhost:8085
          predicates:
            - Path=/employee-service/**
          filters:
            - StripPrefix=1

management:
  endpoints:
    web:
      exposure:
        include: gateway,health
