spring:
  application:
    name: employee-service

  config:
    import: optional:configserver:

  cloud:
    config:
      enabled: false
    import-check:
      enabled: false

server:
  port: 8080
